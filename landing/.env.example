# Copy to .env and fill in.

# Where the waitlist form POSTs { email }. Leave blank to run in demo mode
# (the form still shows a success state without hitting a server).
VITE_WAITLIST_URL=

# Link for the "Install the extension" button. Falls back to "#" when unset.
VITE_EXTENSION_URL=
